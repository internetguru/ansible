#!/bin/bash

home="/home/$(id -un)"

[[ -f "$home/.ansible-info-hide" ]] \
  && exit

ansible-user-dialog "$(</home/$(id -un)/.ansible-info)\n\n<big><b>Show again on next startup?</b></big>" --question \
  || touch "$home/.ansible-info-hide"
