
 # Dependency

  - name: Install dependencies
    apt:
      pkg:
      - software-properties-common

 # Checks

 # Commands

  - name: Install php extensions
    apt:
      pkg:
      - php
      - php-fpm
      - php-common
      - php-mysql
      - php-gd
      - php-bcmath
      - php-json
      - php-xml
      - php-curl
      - php-mbstring

